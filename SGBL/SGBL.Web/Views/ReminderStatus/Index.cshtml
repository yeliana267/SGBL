@model List<SGBL.Application.ViewModels.ReminderStatusViewModel>
@{
    ViewData["Title"] = "Listado de Nacionalidades";
}

<h2 class="mt-3">Nacionalidades</h2>

@if (TempData["ok"] != null)
{
    <div class="alert alert-success">@TempData["ok"]</div>
}

<p>
    <a asp-action="Create" class="btn btn-primary">➕ Nueva nacionalidad</a>
</p>

<table class="table table-bordered table-striped">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var nationality in Model)
        {
            <tr>
                <td>@nationality.Id</td>
                <td>@nationality.Name</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@nationality.Id" class="btn btn-warning btn-sm">Editar</a>
                    <a asp-action="Details" asp-route-id="@nationality.Id" class="btn btn-info btn-sm">Ver</a>
                    <a asp-action="Delete" asp-route-id="@nationality.Id" class="btn btn-danger btn-sm">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
