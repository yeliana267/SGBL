@{
    // No aplicar layout por defecto a las vistas de autenticación
    var controller = ViewContext.RouteData.Values["Controller"]?.ToString();
    if (controller == "AuthViews")
    {
        Layout = null;
    }
    else
    {
        Layout = "_Layout";
    }
}